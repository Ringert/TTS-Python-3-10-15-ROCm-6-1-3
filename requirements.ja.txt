# These cause some compatibility issues on some systems and are not strictly necessary
# japanese g2p deps - NOTE: mecab-python3 requires mecab-config system package (libmecab-dev)
# If you don't need Japanese support, you can skip these dependencies
# To install: apt-get install libmecab-dev mecab mecab-ipadic
# mecab-python3==1.0.6  # DISABLED: Requires mecab-config system package, not available by default
# unidic-lite==1.0.8  # DISABLED: Requires mecab system package
# cutlet  # DISABLED: Depends on mecab-python3
